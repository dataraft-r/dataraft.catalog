% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog-openmetadata.R
\name{dr_catalog_openmetadata}
\alias{dr_catalog_openmetadata}
\title{Publish a table definition to OpenMetadata}
\usage{
dr_catalog_openmetadata(
  endpoint,
  database_schema,
  request = NULL,
  source_tables = NULL
)
}
\arguments{
\item{endpoint}{OpenMetadata server base URL, without \verb{/api/v1}.}

\item{database_schema}{Fully qualified existing database schema, for example
\code{warehouse.analytics.public}.}

\item{request}{Optional authenticated httr2 request or request factory,
as described in \code{\link[=dr_catalog_openlineage]{dr_catalog_openlineage()}}.}

\item{source_tables}{Optional named character vector of source-name to
OpenMetadata table fully qualified name mappings.}
}
\value{
A catalog adapter for \code{\link[dataraft.core:dr_add_catalog]{dataraft.core::dr_add_catalog()}}.
}
\description{
Upserts a table with \code{PUT /api/v1/tables}. The database service, database
and schema must already exist in OpenMetadata. Supported R types are mapped
explicitly to OpenMetadata column types; unsupported types fail before any
request. Descriptions come from the product or contract. This adapter does
not provision infrastructure, transfer data rows, create users, or implement
OpenMetadata's complete governance API.
}
\details{
Optional \code{source_tables} maps source names to existing OpenMetadata table
fully qualified names. The adapter resolves their IDs and adds table-level
lineage with \code{PUT /api/v1/lineage}. Unmapped inputs are omitted; column-level
lineage is not inferred. Repeating table upserts and lineage edges is safe
after an interrupted delivery. Only successful runs publish table metadata.
}
\examples{
catalog <- dr_catalog_openmetadata("https://metadata.example",
  database_schema = "warehouse.analytics.public")
dataraft.core::dr_inspect(catalog)
}
\seealso{
\code{\link[=dr_catalog_openlineage]{dr_catalog_openlineage()}}, \code{\link[dataraft.core:dr_retry_catalogs]{dataraft.core::dr_retry_catalogs()}}
}
