% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog-pane.R
\name{dr_catalog_pane}
\alias{dr_catalog_pane}
\title{Open the read-only catalog in an IDE viewer}
\usage{
dr_catalog_pane(
  lake = NULL,
  snapshot = NULL,
  launch = interactive(),
  refresh_seconds = 30,
  ...
)
}
\arguments{
\item{lake}{Connected lake, or NULL when reading an exported snapshot.}

\item{snapshot}{Path to JSON exported by dr_catalog_export().}

\item{launch}{Start the app; FALSE returns a shiny.appobj for Connect.}

\item{refresh_seconds}{Metadata refresh interval.}

\item{...}{Additional arguments to \code{\link[shiny:runApp]{shiny::runApp()}}, such as \code{port} and
\code{host}. The \code{launch.browser} callback is managed by this function.}
}
\value{
A Shiny app object when \code{launch = FALSE}; otherwise the value
returned when \code{shiny::runApp()} stops.
}
\description{
The viewer receives the actual listening URL from Shiny. If no IDE viewer is
available, the system browser opens instead. Launching runs Shiny in the
current R process and blocks that session until the app stops. Keep a supplied
lake connection open for that time; this function neither disconnects it nor
creates a background server. An exported snapshot can be used independently
of a live lake connection.
}
\examples{
\dontshow{if (requireNamespace("shiny", quietly = TRUE) && requireNamespace("bslib", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
snapshot <- tempfile(fileext = ".json")
writeLines('{"assets":[]}', snapshot)
app <- dr_catalog_pane(snapshot = snapshot, launch = FALSE)
class(app)
unlink(snapshot)
\dontshow{\}) # examplesIf}
}
