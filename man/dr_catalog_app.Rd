% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/catalog.R
\name{dr_catalog_app}
\alias{dr_catalog_app}
\title{Start a read-only data catalog}
\usage{
dr_catalog_app(
  lake = NULL,
  snapshot = NULL,
  launch = interactive(),
  refresh_seconds = 30
)
}
\arguments{
\item{lake}{Connected lake, or NULL when reading an exported snapshot.}

\item{snapshot}{Path to JSON exported by dr_catalog_export().}

\item{launch}{Start the app; FALSE returns a shiny.appobj for Connect.}

\item{refresh_seconds}{Metadata refresh interval.}
}
\value{
A Shiny app object (when launch = FALSE).
}
\description{
Start a read-only data catalog
}
\examples{
\dontshow{if (requireNamespace("duckdb", quietly = TRUE)) (if (getRversion() >= "3.4") withAutoprint else force)(\{ # examplesIf}
root <- tempfile("dataraft-example-")
config <- dataraft.lake::dr_lake_config(
  dataraft.lake::dr_registry_duckdb(file.path(root, "lake.db")),
  dataraft.lake::dr_storage_local(file.path(root, "data")),
  landing = file.path(root, "landing"), backend = "duckdb"
)
lake <- dataraft.lake::dr_connect_lake(config)
app <- dr_catalog_app(lake, launch = FALSE)
class(app)
dataraft.lake::dr_disconnect_lake(lake)
unlink(root, recursive = TRUE)
\dontshow{\}) # examplesIf}
}
